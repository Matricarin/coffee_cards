# 📌 План действий для разработки MVP Coffee Cards (Backend)

## 🗓 Общий план

---

## ✅ Этап 1. Старт проекта
**Цель:** поднять базу проекта и минимальный API.  

1. Создать репозиторий (GitHub/GitLab).  **Сделано**
   - Завести базовую структуру (`Api`, `Domain`, `Application`, `Infrastructure`).  
   - Добавить `.editorconfig`, `.gitignore`, README.  

2. Поднять ASP.NET Core Web API.  **Сделано**
   - `dotnet new webapi -n CoffeeCards.Api`.  
   - Настроить `Program.cs` для Swagger.  
   - Сделать endpoint `/health` (возвращает `OK`).  

3. Описать базовые сущности в Domain.  **Требует корректировки**
   - `User` (Id, Email, PasswordHash).  
   - `CoffeeShop` (Id, Name, LogoUrl).  
   - `Branch` (Id, Address, CoffeeShopId).  
   - `Card` (Id, Title, DiscountType, Conditions, CoffeeShopId).  
   - `Wallet` (UserId, CardId, Status).  

4. API для кофеен (`CoffeeShopController`). **Определен эндпойнт**
   - Создание кофейни.  

5. API для карточек (`CardController`).  **Определен эндпойнт**
   - Создание карточки.  

6. API для кошелька клиента (`WalletController`).  **Определен эндпойнт**
   - Добавление карточки в кошелёк.  
   - Просмотр кошелька.

**Результат недели:**  
- API работает локально.   
- Swagger отображает эндпоинты.  

---

## ✅ Этап 2. Основной функционал MVP
**Цель:** реализовать ключевые сценарии. 

1. Доработка моделей - `Offer`

2. Реализация подготовленных шаблонов эндпойнтов

3. Реализовать маппинг моделей и использовать найденный ранее библиотеки

4. Тесты (xUnit).   

5. Поднять PostgreSQL в Docker.  
   - `docker-compose.yml` с API + PostgreSQL.  
   - Подключить EF Core (code-first).  
   - Создать первую миграцию (`InitialCreate`).

---

## ✅ Этап 3. Доработка и стабилизация
**Цель:** сделать MVP “живым” и готовым к демонстрации.  

1. Добавить бизнес-логику карточек.  
   - Простейшие правила (например: “5 покупок = скидка 10%”).  
   - Фиксация покупки (`TransactionController`).  

2. Реализовать простую статистику.  
   - Количество пользователей у кофейни.  
   - Количество активных карточек.  

3. Настроить логирование.  
   - Подключить Serilog (консоль + файл).  
   - Логировать ошибки и запросы.  

4. Docker & CI/CD  
   - Автосборка образа при пуше в main.  
   - Автодеплой на тестовый сервер (или локально через docker-compose).  

5. Реализовать Auth (MVP).  
   - Регистрация пользователя.  
   - Логин с JWT.  
   - Middleware для защиты эндпоинтов.  

---

# 📊 Результат
Готовый MVP backend с основным функционалом.
Можно отдать фронтендеру (или использовать заглушки).
Можно показать как демо потенциальным кофейням. 

---


# Обновления и вопросы

## 2025-09-12

- Обсудить и добавить следующие проверки работоспособности:
	1. Проверки готовности и активности
	2. Проверка базы данных
	3. Проверка ORM

## 2025-09-14

Разделить Offer (предложение) и Card (экземпляр у пользователя).
Как фиксировать покупку и прогресс по карте?
Определить роли пользователей (Client, CoffeeShop, Employee, Admin).
Нужно ли состояние карточки?
